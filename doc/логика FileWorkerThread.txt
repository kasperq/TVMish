вставка локального файла
	-запускаем поток для работы с файлом
		-определяем полный путь к файлу
		-определяем расширение файла
		-запрашиваем валидность расширения файла в формате (index, fullPath, extension)
			-передаем сигнал в основной поток
			-он передает его через сигнал в гэйтвэй
			-гэйтвэй возвращает результат через сигнал в основной поток
			-основной поток через сигнал передает результат в WorkerThread
		
		ЖДЕМ
		
		-валидность получена (index, fullPath, extension, isValid)
		-если расширение валидное
		-берем путь к файлу
		-берем имя файла
		-берем доступность файла=true
		-определяем путь в локальную папку с проги
		-копируем файл
		-если удачно
			-вызываем сигнал об удачном копировании
			
	-в основном потоке
		-в настройках
			-заносим короткий путь в настройки как последний путь
		-в классе файлов PlFiles заносим полученную информацию в item по полученному индексу
		
		
		
		if (item.fileName() == "")
                item.setFileName(fName);
            item.setFilePath(plFilePath);
            item.setFilePathLocal(newFilePath);
            item.setFormat(fileExt);
            item.setIdFormat(idFormat);
            item.setIsAvailable(isAvailable);
			
			


